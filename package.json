{
  "name": "bg-clean-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:server": "cd apps/server && bun --hot server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
} 